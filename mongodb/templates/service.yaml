apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: roboshop
  labels:
    app: mongodb
    project: roboshop
    tier: db
spec:
  ports:
  - port: 27017
    name: mongodb
    protocol: TCP          # ✅ Corrected spelling & capitalization
  clusterIP: None
  selector:
    app: mongodb
    project: roboshop
    tier: db